// testing on fastq files
params {
    // Limit resources so that this can run on CI
    max_cpus   = 2
    max_memory = '6.GB'
    max_time   = '6.h'

    // Pipeline params
    debug = true
    fastq = './data/fastq'
}